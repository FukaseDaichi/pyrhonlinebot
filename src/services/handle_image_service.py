
class HandleImageService :

	@staticmethod
	def generate_reply_message(message_content) :

    	# message_idから画像のバイナリデータを取得
		# with open(Path(f"static/images/{message_id}.jpg").absolute(), "wb") as f:
        # 	# バイナリを1024バイトずつ書き込む
		# 	for chunk in message_content.iter_content():
        #     	f.write(chunk)
		return 